# metapi 優化清單

> 目標：先修公開 issue 裡已知的 bug，再做結構性優化。  
> 來源：本地架構掃描 + GitHub 公開 issue／PR 快照 + git ancestry、fork commit 與 regression evidence（2026-08-15）。
> 狀態口徑：本文的 ✅ 只代表目前 fork main 已有具體修正與回歸覆蓋，不代表 upstream issue／PR 已關閉或已被直接合併；第 3 節列出的 17 個 issue 截至 2026-08-15 在 upstream 仍全部為 **OPEN**。

## 0. Fork 維護定位與已修正清單

### 維護政策：由 fork 獨立演進

- [`yswlww/metapi-evolution`](https://github.com/yswlww/metapi-evolution) 是源自 [`cita-777/metapi`](https://github.com/cita-777/metapi) 的社群維護、非官方獨立延續版本，並非原作者或 upstream repository 認可的官方續作。
- 目前 fork 已包含多項 upstream 尚未合併或驗收的修正；其中部分與 upstream PR 功能等價，但 commit ancestry、實作細節與測試均屬本 fork 的獨立工作。
- 自 2026-08-15 起，本 fork **不再直接 merge upstream branch、PR 或完整 commit 序列**。Upstream 只作為需求與實作參考；有價值的變更必須在本 fork 重新評估、獨立實作、補回歸測試並完成本地 review 後才會採用。
- Upstream issue／PR 的 OPEN、CLOSED 或 MERGED 狀態不再作為本 fork 是否已處理的唯一判斷；本文件以 fork main 的 code、commit 與 regression evidence 為準。
- `metapi-evolution` repository 已建立並作為本獨立分支的維護入口；產品名稱仍為 `metapi`，Docker image 仍為 `kennethww/metapi`，desktop app ID 仍為 `me.cita777.metapi.desktop`。

### Upstream Pull Request 處理狀態（2026-08-15）

截至 2026-08-15，[`cita-777/metapi` Pull Requests](https://github.com/cita-777/metapi/pulls) 有目前 11 個 OPEN upstream PR。經 git ancestry、patch、現行 code 與 tests 對照，**沒有任何一個是直接 merge** 到 fork `main`；「等價功能已獨立實作」只代表需求結果已在 fork 以不同 commit／實作落地，不代表採用了 upstream PR 的 commit series。

| Upstream PR | Fork 狀態 | Fork evidence／處理決定 |
|---|---|---|
| [#602](https://github.com/cita-777/metapi/pull/602) | ✅ 等價功能已獨立實作 | commit `c939772` 完整轉發 Compose 文件化環境變數、保留 `AUTH_TOKEN`／`PROXY_TOKEN` guards，並讓 `PORT` 同時控制 host／container port；`docker.workflow.test.ts` 有完整 contract。 |
| [#601](https://github.com/cita-777/metapi/pull/601) | ✅ 等價功能已獨立實作 | commit `c939772` 新增並正規化 `TELEGRAM_API_BASE_URL`，保留官方 API fallback；`config.test.ts` 覆蓋 default、空白及 trailing slash。 |
| [#599](https://github.com/cita-777/metapi/pull/599) | ✅ 等價維護已獨立重做 | commit `a69f35b` 已採用 `setup-node@v7`、`labeler@v7`、`cache@v6`；upstream Dependabot head 並非 fork ancestry。 |
| [#596](https://github.com/cita-777/metapi/pull/596) | ✅ 等價功能已獨立實作 | commit `c68f8c5` 實作 hardened Helm `existingSecret`；external mode 不產生 managed Secret／checksum，並有 k3s asset regression。 |
| [#588](https://github.com/cita-777/metapi/pull/588) | 🟡 部分覆蓋 | commits `962e5e2`、`591168d`、`73ce9fe` 以 stable diff、atomic identity 與 reconciliation owner 獨立實作核心 route sync；未採用 delete-all/reinsert、額外 GHCR workflow，route hiding 則早已有獨立實作。 |
| [#584](https://github.com/cita-777/metapi/pull/584) | ✅ 等價功能已獨立實作 | commits `21207b5`、`6364597`、`28868a4` 實作 custom-header priority、schema／backup／proxy path，另加 credential 與 hop-by-hop transport hardening。 |
| [#581](https://github.com/cita-777/metapi/pull/581) | 🟡 部分覆蓋 | Gemini thought-signature transformer 與 regressions 已存在；但 official Gemini `/v1/chat/completions` 自動切換 native `generateContent` 的 PR-specific routing bridge 尚未落地，不能標成完整等價。 |
| [#575](https://github.com/cita-777/metapi/pull/575) | ✅ 等價功能已獨立實作 | commit `4a62eba` 以 dialect-aware MySQL／TiDB duplicate-key upsert 修正 admin snapshot，並有 `adminSnapshotStore.mysql.test.ts`。 |
| [#557](https://github.com/cita-777/metapi/pull/557) | ❌ 未採用 | Node 22 → 26 會破壞既定 `linux/arm/v7` 支援；Dockerfile 保持 `node:22-bookworm-slim`，`docker.workflow.test.ts` 鎖定此部署契約。 |
| [#550](https://github.com/cita-777/metapi/pull/550) | 🟡 部分覆蓋 | New API cookie／user-ID compatibility 已由 commits `cac9f77`、`1d6a83f` 獨立實作；downstream key 採較嚴格的「空集合 deny-all＋UI 明確填滿」設計，context length／manual model delete 未採用。 |
| [#520](https://github.com/cita-777/metapi/pull/520) | ❌ 未採用 | `context_length` propagation/cache 與 manual model delete 仍未落地，已明確延後拆分評估，不能視為已合併或已完成。 |

另有兩個與既有 fork 工作直接相關的歷史 upstream PR：

- [#470](https://github.com/cita-777/metapi/pull/470)：upstream 已關閉但未合併；fork commit `8cf4507` 只獨立採用 dashboard balance truncation，沒有採用其大範圍 CSS diff。
- [#567](https://github.com/cita-777/metapi/pull/567)：其後已在 upstream 合併，但 upstream merge commit 未進入 fork ancestry；fork commit `1d6a83f` 已獨立提供更廣的 New API user-ID header probing／compatibility。

### metapi-evolution 本倉庫 PR 與保護規則

> 本節的 PR #1／PR #2 均屬 `yswlww/metapi-evolution`，不是上述 `cita-777/metapi` upstream PR。

| PR／設定 | 狀態 | 更新內容 |
|---|---|---|
| [PR #1](https://github.com/yswlww/metapi-evolution/pull/1) | ✅ 已關閉，未合併 | Dependabot 提議把 Docker base 升至 Node 26；Node 22／ARMv7 contract test 正確阻止變更。PR #2 合併 ignore 規則後，Dependabot 自動關閉此 PR。 |
| [PR #2](https://github.com/yswlww/metapi-evolution/pull/2) | ✅ 已合併 | 重寫中英文 Origin and Evolution，明確標示非官方社群延續版本；新增 Docker Node major-update ignore 與文件契約測試。15 項 checks 全部通過，merge commit `154ea82`。 |
| [`protect-main`](https://github.com/yswlww/metapi-evolution/rules/20888216) | ✅ Active | `main` 禁止刪除及 force push，所有變更必須經 PR 並通過指定 tests、build、typecheck、drift 與 schema checks。 |
| [`protect-release-tags`](https://github.com/yswlww/metapi-evolution/rules/20888217) | ✅ Active | `v*` release tags 禁止更新及刪除，仍允許建立後續版本標籤。 |

### 17 個 roadmap issues：fork main 已修正明細

| Issue | Fork 狀態 | 修正來源 | 已落地內容 | Upstream 狀態 |
|---|---|---|---|---|
| [#573](https://github.com/cita-777/metapi/issues/573) | ✅ 已修正 | 較早 fork commit `49f1e9d` | 未識別站點建立／偵測失敗改為明確 HTTP 400，不再假成功 | OPEN |
| [#565](https://github.com/cita-777/metapi/issues/565) | ✅ 已修正 | 較早 fork commit `49f1e9d` | Token label、default 與 enabled 狀態在同步後保持 | OPEN |
| [#568](https://github.com/cita-777/metapi/issues/568) | ✅ 已修正 | 較早 fork commits `67f7464`、`e470a3d` | Retryable 5xx／503 與 proxy-only key 錯誤不再誤判 account expired | OPEN |
| [#571](https://github.com/cita-777/metapi/issues/571) | ✅ 已修正 | 較早 fork commit `cac9f77` | 更新 Codex client identity／headers，兼容較新模型要求 | OPEN |
| [#538](https://github.com/cita-777/metapi/issues/538) | ✅ 已修正 | 較早 fork commit `cac9f77` | 移除不應回傳的 reasoning history，同時保留 tool items | OPEN |
| [#566](https://github.com/cita-777/metapi/issues/566) | ✅ 已修正 | 較早 fork commit `cac9f77` | 放寬 SUB2API latency matching 並恢復 `actual_cost` 回填 | OPEN |
| [#555](https://github.com/cita-777/metapi/issues/555) | ✅ 已修正 | 較早 fork commit `cac9f77` | Token total 納入 cache read／creation tokens | OPEN |
| [#496](https://github.com/cita-777/metapi/issues/496) | ✅ 已修正 | 較早 fork commit `cac9f77` | AnyRouter Claude cache pricing fallback 使用正確 0.1／1.25 倍率 | OPEN |
| [#504](https://github.com/cita-777/metapi/issues/504) | ✅ 已修正 | 較早 fork commit `cac9f77` | 辨識 `previous_response_id` compatibility error 並進入移除參數重試 | OPEN |
| [#569](https://github.com/cita-777/metapi/issues/569) | ✅ 已修正 | 較早 fork commit `7df0326` | 新增連接時 proxy 設定會送出、保存並套用 | OPEN |
| [#560](https://github.com/cita-777/metapi/issues/560) | ✅ 已修正 | 較早 fork commit `cac9f77` | New API cookie check-in 傳送完整 fork user-ID compatibility headers | OPEN；PR #550 僅有部分重疊範圍 |
| [#526](https://github.com/cita-777/metapi/issues/526) | ✅ 已修正 | 本輪 commits `962e5e2`、`591168d`、`73ce9fe` | 新增／修改 channel 後統一同步 pattern routes | OPEN；PR #588 核心範圍已獨立實作 |
| [#530](https://github.com/cita-777/metapi/issues/530) | ✅ 已修正 | 本輪 commits `21207b5`、`6364597`、`28868a4` | 自訂 header priority、持久化、proxy path 與認證／transport 保護 | OPEN；PR #584 仍未合併，fork 為獨立實作 |
| [#559](https://github.com/cita-777/metapi/issues/559) | ✅ 已修正 | 本輪 route reconciliation 系列 | Regex／wildcard routes 在拓撲 mutation 後重新同步 | OPEN；PR #588 核心範圍已獨立實作 |
| [#462](https://github.com/cita-777/metapi/issues/462) | ✅ 已修正 | 較早 fork commit `cac9f77` | 100+ OAuth accounts 改用 server-side batch delete 與單次 rebuild | OPEN |
| [#463](https://github.com/cita-777/metapi/issues/463) | ✅ 已修正 | 較早 fork commit `cac9f77` | Desktop 注入實際 app version，不再固定顯示 v0.0.0 | OPEN |
| [#405](https://github.com/cita-777/metapi/issues/405) | ✅ 已修正 | 較早／upstream-derived commit `ace5ddd` | Nullable `maxRequests` 可真正清空並持久化 quota | OPEN |

> 上表是目前 fork main 已有實作與專項 regression evidence 的 17 項。#511、#359、#493 等未列入第 3 節 roadmap 的問題仍屬另外的待確認／待處理範圍，不能因上表完成而視為已修正。

### 本輪新增整合內容

本輪以 `1d6a83f` 為整合基準；17 個 roadmap issue 的修正目前都已存在於 fork main，但只有 #526 / #530 / #559 是本輪分支新增，其餘 14 項在本輪開始前已有對應修正。公開 issue 是否關閉仍以上游逐項驗收為準。

### 路由與站點請求頭

- ✅ [#526](https://github.com/cita-777/metapi/issues/526) 新增渠道後，pattern route 會透過統一 reconciliation owner 同步衍生渠道。
- ✅ [#559](https://github.com/cita-777/metapi/issues/559) wildcard / regex 群組會在 exact route 拓撲變更後重新同步，涵蓋新增、更新、停用、刪除與批量操作。
- ✅ [#530](https://github.com/cita-777/metapi/issues/530) 站點自訂請求頭新增可選優先權，並保留既有 User-Agent 相容行為；認證及 hop-by-hop headers 仍由實際請求控制。
- 路由同步改為 canonical candidate policy，統一 site/global filters、managed-token、direct-account 與 OAuth route-unit 候選來源。
- route/channel mutation、exact population、pattern reconciliation 與 snapshot invalidation 現在共用 atomic transaction boundary。
- 新增 `automatic_identity` 與資料庫 unique invariant，避免 rolling update 或多 process 同時建立重複 automatic channels；舊資料會在 reconciliation 時原地正規化並保留 ID/telemetry。

### CPA pricing alias follow-up

- pricing boundary 改用共用 `normalizePlatformAlias()`：`cliproxyapi`、大小寫/空白變體、`cpa` 與 `cli-proxy-api` 都不再呼叫不支援的 `/api/pricing`。
- 站點 POST / PUT 會把已知 CPA alias 持久化為 `cliproxyapi`；歷史 alias rows 由 pricing boundary 立即保護，不做破壞性資料回填。
- `new-api` 保持原有 pricing 行為；不能由名稱安全推斷為 CPA，既有誤分類資料仍需人工修正。
- 本 follow-up 已完成 estimate、cached catalog、forced refresh 與站點 create/update 的本地回歸；尚未宣稱 upstream 驗收或對應 issue 已關閉。

### Schema、部署與維護

- 新增 site custom-header priority 與 route-channel automatic identity 的 additive migrations，並同步 SQLite contract、MySQL/PostgreSQL bootstrap 與 upgrade artifacts。
- Database-to-database migration、backup/import 與 OAuth rollback 路徑保留新增欄位及既有 route-unit / post-refresh probe 資料。
- Docker Compose 現在完整轉發文件化環境變數，`PORT` 同時控制 published/target port，`AUTH_TOKEN` / `PROXY_TOKEN` required guards 保持不變。
- Telegram API base URL 可透過環境變數覆寫並正規化，持久化 runtime settings 仍具有較高優先權。
- Helm chart 新增 hardened `existingSecret` 模式：外部 Secret 模式不產生 chart Secret/checksum，managed 模式及 required validations 維持原行為。
- GitHub Actions 已完成 `setup-node@v7`、`labeler@v7`、`cache@v6` 的精確版本更新，未改動 triggers、permissions 或發布流程。

### 驗證結果

- 完整測試：2724 passed，8 skipped。
- Web / web-test / server / desktop typecheck：通過。
- Web / server / desktop production build：通過。
- Repository drift：0 violations，0 tracked debt。
- 完整 branch diff check：通過。
- 最終 Docker image 本機 build：通過；一次性容器對 `/` 回應 HTTP 200，container 與 volume 均已移除。
- 最終 whole-branch review：所有 Critical / Important findings 已處理；僅保留非阻塞的 architecture-test 強度與 mixed-version rollout 操作風險。

---

## 1. 已知 bug 優先清單

### P0 — 直接影響可用性 / 會誤導用戶
- ✅ [#573](https://github.com/cita-777/metapi/issues/573) 新增站點在不識別 URL 時，後端回 200 但前端仍顯示成功，實際未落庫。**目前 fork main 已有對應修正與回歸；upstream 仍 OPEN。**
- ✅ [#571](https://github.com/cita-777/metapi/issues/571) Codex OAuth 後無法正常調用 `gpt-5.5`，上游回 400，影響新模型可用性。**目前 fork main 已有對應修正與回歸；upstream 仍 OPEN。**
- ✅ [#569](https://github.com/cita-777/metapi/issues/569) 「添加连接」面板缺少代理配置，導致部分站点无法连通。**目前 fork main 已有對應修正與回歸；upstream 仍 OPEN。**
- ✅ [#568](https://github.com/cita-777/metapi/issues/568) 中转站 key 会被误置为 `expired`，影响 Claude 调用稳定性。**目前 fork main 已有對應修正與回歸；upstream 仍 OPEN。**
- ✅ [#566](https://github.com/cita-777/metapi/issues/566) SUB2API 在特定延迟场景下成本回填失败，进而污染路由成本。**目前 fork main 已有對應修正與回歸；upstream 仍 OPEN。**
- ✅ [#565](https://github.com/cita-777/metapi/issues/565) 账号刷新后默认 Key 名被强制改成 `default`，同步还会覆盖启用/禁用状态。**目前 fork main 已有對應修正與回歸；upstream 仍 OPEN。**
- ✅ [#538](https://github.com/cita-777/metapi/issues/538) `/v1/responses` 多輪對話第二輪失敗，reasoning item 轉發/歸一化有問題。**目前 fork main 已有對應修正與回歸；upstream 仍 OPEN。**
- ✅ [#555](https://github.com/cita-777/metapi/issues/555) Token 統計不準確，疑似漏算 cache 相關 token。**目前 fork main 已有對應修正與回歸；upstream 仍 OPEN。**

### P1 — 影響核心管理流程
- ✅ [#560](https://github.com/cita-777/metapi/issues/560) 公益站簽到報 `Unauthorized`，但手動簽到可成功。**目前 fork main 已有對應修正與回歸；upstream 仍 OPEN。**
- ✅ [#559](https://github.com/cita-777/metapi/issues/559) 路由群組用正則後，新加入站點不會自動納入。**本輪分支新增對應同步修正與回歸；upstream 仍 OPEN。**
- ✅ [#526](https://github.com/cita-777/metapi/issues/526) 新增渠道後，已有群組不會自動添加路由。**本輪分支新增對應同步修正與回歸；upstream 仍 OPEN。**
- ✅ [#530](https://github.com/cita-777/metapi/issues/530) 站點管理裡的自訂請求頭 / UA 沒有生效。**本輪分支新增 priority、持久化與 proxy-path 修正；upstream 仍 OPEN。**
- [#511](https://github.com/cita-777/metapi/issues/511) MiniMax 請求會把 think 內容拼進最終輸出。
- ✅ [#504](https://github.com/cita-777/metapi/issues/504) Codex 返回 `previous_response_id` 不支援。**目前 fork main 已有對應修正與回歸；upstream 仍 OPEN。**
- ✅ [#496](https://github.com/cita-777/metapi/issues/496) AnyRouter Claude 緩存定價回退不正確，pricing 失真。**目前 fork main 已有對應修正與回歸；upstream 仍 OPEN。**

### P2 — 體驗 / 性能 / 可維護性問題
- ✅ [#462](https://github.com/cita-777/metapi/issues/462) OAuth 管理批量刪除 100+ 帳號時卡頓、失敗率高。**目前 fork main 已有對應修正與回歸；upstream 仍 OPEN。**
- ✅ [#463](https://github.com/cita-777/metapi/issues/463) 桌面版版本檢測一直提示 v0.0.0。**目前 fork main 已有對應修正與回歸；upstream 仍 OPEN。**
- ✅ [#405](https://github.com/cita-777/metapi/issues/405) 編輯下游密鑰時清空請求額度不生效。**目前 fork main 已有對應修正與回歸；upstream 仍 OPEN。**
- [#359](https://github.com/cita-777/metapi/issues/359) 連接已 expired，但列表仍顯示健康。
- [#493](https://github.com/cita-777/metapi/issues/493) WebDAV 匯出失敗，需確認 409 / 匯出路徑處理。

---

## 2. 本地架構掃描得到的優化點

### 後端主鏈路
- `src/server/index.ts` 啟動流程很長，建議把「資料庫初始化 / runtime settings / data repair / task startup」拆成更清晰的階段日誌，方便排障。
- `src/server/proxy-core/` 是最關鍵的性能與穩定性核心，優先圍繞：
  - 路由選擇
  - 通道黏性
  - 重試策略
  - 首字超時
  - 協議轉換
- Pattern route reconciliation 已集中到 stable orchestration boundary；後續應持續監控大型 full rebuild transaction 的鎖定時間。
- `src/server/db/` 的跨 dialect 路徑已有更多 schema/migration regression coverage，但 MySQL/PostgreSQL 的 live concurrent identity 測試仍值得補充。

### 前端
- `src/web/App.tsx` 與幾個大型頁面（Accounts / Settings / ModelTester / Sites / TokenRoutes）仍然偏大，後續建議繼續拆分頁面內邏輯。
- 當前沒有通用請求緩存層，重複拉取列表與狀態時可考慮局部 cache 或 request deduplication。
- 事件輪詢、session check、notification panel 都依賴 timer，建議統一收斂 polling strategy，減少無效刷新。

### Desktop
- `src/desktop/main.ts` 負責託管後端、tray、update 與 crash recovery，建議統一「啟動失敗 / 運行中崩潰 / 重啟後端」三條路徑的日誌格式。
- 更新檢測與後端 health check 屬於高頻體驗問題，適合先做更細的 error classification。

### 運維 / 可觀測性
- `PROXY_DEBUG_TRACE_ENABLED` 等調試路徑已存在，問題復現時應優先以它建立 evidence chain。
- 路由成本、token 統計、model availability 與 downstream key status 屬於同一類決策信號，建議統一校驗口徑。
- `existingSecret` 採用 external Secret 時，rotation 後需要手動 rollout；mixed-version rolling update 期間應避免拓撲 mutation，或在 rollout 後執行一次 route rebuild 以正規化舊 binary 可能寫入的 NULL automatic identities。

---

## 3. Roadmap issue 狀態與上游驗收順序

以下 17 個 roadmap issue 在目前 fork main 都已有具體修正與回歸覆蓋；截至 2026-08-15，它們在 upstream 仍全部為 **OPEN**，因此後續工作是逐項重現、提交與驗收，而不是再把它們描述為 fork 內尚未修正：

1. **直接影響可用性 / 會誤導用戶**：#573 / #565 / #568 / #571 / #538
2. **路由、統計與協議誤差**：#566 / #555 / #496 / #504
3. **管理流程缺口**：#569 / #560 / #526 / #530 / #559
4. **體驗問題**：#462 / #463 / #405

其中只有 #526 / #530 / #559 來自本輪分支；其餘 14 項在本輪整合前已存在於 fork main。建議 upstream 仍依上述風險順序逐項驗收，並在 issue 真正關閉前維持 OPEN 標記。

---

## 4. 一句話結論

目前 fork main 已具備第 3 節全部 17 個 roadmap issue 的具體修正，也補上 CPA pricing alias boundary 與站點持久化防線；所有 17 個 upstream issue 仍待逐項驗收且保持 OPEN。對目前 11 個 OPEN upstream PR 的準確結論是：6 個等價功能已獨立實作（#575／#584／#596／#599／#601／#602）、3 個部分覆蓋（#550／#581／#588）、2 個未採用（#520／#557），直接 merge 數量為 0；另外 #470／#567 亦是歷史上的獨立等價實作，而非直接 merge。
